import { memo } from "react";
import styled from "styled-components";
import { SearchHeader } from "./tabs";

export const Serach = memo(() => {
  return (
    <Wrapper0>
      <SearchHeader />
    </Wrapper0>
  );
});

const Wrapper0 = styled.div`
  height: 100vh;
`;
